# Pokemon-db-project

Tech Stack:
- Python
- Flask (backend)
- SQLite 
- TCGdex API (switch from scrydex as that was a paid api)
- uv (for dependency management)

uv usage:
uv sync -- install all dependencies locally
uv add {dependency} -- add to dependency list
uv run {filename.py} -- run files with dependencies